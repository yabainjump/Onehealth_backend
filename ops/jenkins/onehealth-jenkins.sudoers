# Install as /etc/sudoers.d/onehealth-jenkins with mode 0440.
# The isolated build agent can execute one immutable wrapper as the
# unprivileged application user. The Jenkins controller receives no sudo rule.
onehealthci ALL=(yabain) NOPASSWD: /usr/local/sbin/deploy-onehealth-backend
